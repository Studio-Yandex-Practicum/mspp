name: Stage Deploy

concurrency:
  group: stage_deploy
  cancel-in-progress: true

on:
  push:
    branches:
      - develop
      - 'feature/**'
      - 'fix/**'
  pull_request:
    branches:
      - develop
    types: [closed]

  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: mssp
  DEPLOY_PATH: /MSPP

defaults:
  run:
    working-directory: .

jobs:
  build-and-push-image-to-github-packages:
    name: Push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    environment:
      name: stage_deploy
    permissions:
      contents: read
      packages: write
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: develop
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Sanitize repo slug
        uses: actions/github-script@v6
        id: repo_slug
        with:
          result-encoding: string
          script: return 'ghcr.io/${{ github.repository }}'.toLowerCase()
      - name: Push Stage to GitHub Container Registry
        uses: docker/build-push-action@v4
        with:
          context: "{{defaultContext}}"
          push: true
          tags: ${{ steps.repo_slug.outputs.result }}:stage

  deploy:
    name: Deploy changes on server
    runs-on: ubuntu-latest
    environment:
      name: stage_deploy
    needs: build-and-push-image-to-github-packages
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Sanitize repo slug
        uses: actions/github-script@v6
        id: repo_slug_deploy
        with:
          result-encoding: string
          script: return 'ghcr.io/${{ github.repository }}'.toLowerCase()
      - name: Create directory for deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.PASSPHRASE }}
          script: |
            sudo mkdir -p ${{ env.DEPLOY_PATH }}
            sudo chmod 777 ${{ env.DEPLOY_PATH }}
      - name: copy file via ssh
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.PASSPHRASE }}
          source: "infra/docker-compose.staging.yml"
          target: ${{ env.DEPLOY_PATH }}
      - name: executing remote ssh commands to deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.PASSPHRASE }}
          script: |
            cd ${{ env.DEPLOY_PATH }}

            echo SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }} > infra/.env
            echo ALLOWED_HOSTS=${{ secrets.DJANGO_ALLOWED_HOSTS }} >> infra/.env

            echo GOOGLE_PRIVATE_KEY_ID=${{ secrets.GOOGLE_PRIVATE_KEY_ID }} >> infra/.env
            echo GOOGLE_PRIVATE_KEY=${{ secrets.GOOGLE_PRIVATE_KEY }} >> infra/.env

            echo WEBHOOK_MODE=${{ secrets.WEBHOOK_MODE }} >> infra/.env
            echo WEBHOOK_URL=${{ secrets.WEBHOOK_URL }} >> infra/.env

            echo POSTGRES_DB=${{ secrets.POSTGRES_DB }} >> infra/.env
            echo POSTGRES_USER=${{ secrets.POSTGRES_USER }} >> infra/.env
            echo POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }} >> infra/.env
            echo POSTGRES_HOST=${{ secrets.POSTGRES_HOST }} >> infra/.env
            echo POSTGRES_PORT=${{ secrets.POSTGRES_PORT }} >> infra/.env
            echo TELEGRAM_TOKEN=${{ secrets.TELEGRAM_TOKEN }} >> infra/.env

            sudo docker compose -f infra/docker-compose.staging.yml stop
            sudo docker compose -f infra/docker-compose.staging.yml rm -f
            sudo docker rmi $(sudo docker images -qa)
            sudo docker compose -f infra/docker-compose.staging.yml up -d --quiet-pull

            sudo docker compose -f infra/docker-compose.staging.yml exec backend python manage.py migrate
            sudo docker compose -f infra/docker-compose.staging.yml exec backend python manage.py fill_data
